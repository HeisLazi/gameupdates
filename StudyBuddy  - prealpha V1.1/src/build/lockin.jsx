/* ============ DEPRECATED — LOCK IN MODE REMOVED ============ *
 * Folded into Practice (no-stakes single fight from the World view).
 * This file is intentionally empty so it doesn't redeclare LockInHub.
 * It has also been removed from build_html.py extra_files; left here as a
 * tombstone so old patches don't accidentally re-add it.
 */
